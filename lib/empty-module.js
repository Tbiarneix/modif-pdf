// Module vide : cible d'alias pour la dépendance optionnelle `canvas` de
// pdfjs-dist (utilisée uniquement côté Node, inutile dans le build navigateur).
const emptyModule = {};
export default emptyModule;
